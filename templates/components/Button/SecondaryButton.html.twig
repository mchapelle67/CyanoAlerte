<button
    class="backdrop-blur-md bg-white/20 text-white px-8 py-4 rounded-xl border-2 border-white/30 hover:bg-white/30 hover:-translate-y-1 transition-all duration-500 ease-in-out {{ class }}" 
    type="{{ type }}"
    >
    {{ text }}
</button>